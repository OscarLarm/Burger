#docker compose -f ./Containers/MenuStore/docker-compose.yml up

version: '3.1'

services:

  mongo:
    image: mongo
    container_name: MenuStore
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: BurgerPassword
    ports:
      - 27017:27017
    volumes:
      - ./MenuStoreData.json:/MenuStoreData.json
    
